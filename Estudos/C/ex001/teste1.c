#include <stdio.h>

int main(){
    int x, y;

    x = 10;
    y = x++;
    printf("y = x++ : valor de y => %d\n", y);
    printf("y = x++ : valor de x => %d\n\n", x);

    x = 10;
    y = x--;
    printf("y = x-- : valor de y => %d\n", y);
    printf("y = x-- : valor de x => %d\n\n", x);

    x = 10; y = ++x;
    printf("y = ++x : valor de y => %d\n", y);
    printf("y = ++x : valor de x => %d\n\n", x);

    x = 10, y = --x;
    printf("y = --x : valor de y => %d\n", y);
    printf("y = --x : valor de x => %d\n\n", x);
} 

1D
2B
3A
4
5 iniciar;
declarar variaveis;
funcionario;
nao funcionario;
cadastro;
salario;
se nao funcionario; 
    encerre programa;
se funcionario;
    cadastre;
se funcionario cadastrado;
    salario/100*30;
encerrar programa;

2

1D

